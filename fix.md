# 🔧 TAKARA PLATFORM AUDIT & FIX — ПОЛНЫЙ АУДИТ И ИСПРАВЛЕНИЕ

**Role:** Senior Full-Stack Developer & QA Engineer  
**Priority:** CRITICAL  
**Objective:** Привести платформу в 100% рабочее состояние  
**Repository:** https://github.com/olegin77/TUSD  
**Production:** https://143.198.17.162/vaults

---

## 📋 EXECUTIVE SUMMARY

Платформа TAKARA имеет множество нерабочих элементов: кнопки не работают, логика не полностью реализована, некоторые компоненты показывают ошибки.

**Твоя задача:** Провести полный аудит платформы, найти ВСЕ проблемы и исправить их. Платформа должна работать на 100% (за исключением реальных blockchain транзакций, которые требуют задеплоенных смарт-контрактов).

---

## 🔢 ЧАСТЬ 0: ПРАВИЛЬНЫЕ ЗНАЧЕНИЯ APY (ФИНАЛЬНЫЕ!)

### ⚠️ КРИТИЧЕСКИ ВАЖНО: Обнови ВСЕ места где указаны APY!

| Vault | Name     | Start APY (без буста) | Boost          | Max APY  | Срок   | Мин. вход |
| ----- | -------- | --------------------- | -------------- | -------- | ------ | --------- |
| **1** | Starter  | **4.5%**              | Laika **+4%**  | **8.5%** | 12 мес | $100      |
| **2** | Advanced | **7%**                | Takara **+6%** | **13%**  | 30 мес | $1,500    |
| **3** | Whale    | **8%**                | Takara **+7%** | **15%**  | 36 мес | $5,000    |

### Где нужно обновить:

1. **Database (Prisma seed)**
2. **Backend (конфиги, калькуляторы)**
3. **Frontend (карточки Vaults, калькуляторы, отображение)**
4. **API responses**

---

## 🔍 ЧАСТЬ 1: МЕТОДОЛОГИЯ АУДИТА

### 1.1 Порядок действий

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        AUDIT WORKFLOW                                    │
└─────────────────────────────────────────────────────────────────────────┘

STEP 1: ENVIRONMENT CHECK
├── Проверить что сервис запускается без ошибок
├── Проверить подключение к БД
├── Проверить все environment variables
└── Проверить build без ошибок

STEP 2: DATABASE AUDIT
├── Проверить schema.prisma на ошибки
├── Проверить seed data (особенно APY!)
├── Проверить миграции
└── Проверить что данные корректны в БД

STEP 3: BACKEND AUDIT
├── Проверить все endpoints (GET, POST, PUT, DELETE)
├── Проверить все services на ошибки
├── Проверить error handling
├── Проверить валидацию данных
└── Тестировать каждый endpoint через curl/Postman

STEP 4: FRONTEND AUDIT
├── Открыть КАЖДУЮ страницу
├── Нажать КАЖДУЮ кнопку
├── Проверить КАЖДУЮ форму
├── Проверить console на ошибки
├── Проверить network requests
└── Проверить responsive design

STEP 5: INTEGRATION AUDIT
├── Проверить связь Frontend ↔ Backend
├── Проверить auth flow
├── Проверить data flow
└── End-to-end тестирование
```

### 1.2 Как документировать найденные проблемы

При обнаружении проблемы, записывай в формате:

```
ISSUE #XX: [Краткое описание]
├── Location: [Файл и строка]
├── Type: [BUG | INCOMPLETE | MISSING | WRONG_DATA]
├── Severity: [CRITICAL | HIGH | MEDIUM | LOW]
├── Description: [Подробное описание]
├── Expected: [Ожидаемое поведение]
├── Actual: [Текущее поведение]
└── Fix: [Как исправить]
```

---

## 🗄️ ЧАСТЬ 2: DATABASE AUDIT

### 2.1 Проверка Schema

```bash
# Выполни эти команды и проверь output:

cd /path/to/project

# 1. Проверить schema на ошибки
npx prisma validate

# 2. Проверить статус миграций
npx prisma migrate status

# 3. Сгенерировать клиент
npx prisma generate

# 4. Открыть Prisma Studio для визуальной проверки
npx prisma studio
```

### 2.2 Checklist для Database

- [ ] Schema валидна (prisma validate проходит)
- [ ] Все миграции применены
- [ ] Prisma Client генерируется без ошибок
- [ ] Таблица Vault/Pool существует и содержит данные
- [ ] APY значения ПРАВИЛЬНЫЕ:
  - [ ] Vault 1: baseApyBps = 450, boostApyBps = 400, maxApyBps = 850
  - [ ] Vault 2: baseApyBps = 700, boostApyBps = 600, maxApyBps = 1300
  - [ ] Vault 3: baseApyBps = 800, boostApyBps = 700, maxApyBps = 1500
- [ ] Все required поля заполнены
- [ ] Нет orphaned records
- [ ] Foreign keys корректны

### 2.3 Исправление Seed Data

Найди файл seed (обычно `prisma/seed.ts` или `prisma/seeds/*.ts`) и обнови:

```typescript
// prisma/seed.ts или аналогичный файл

// VAULT 1: Starter
{
  name: 'Starter',
  type: 'VAULT_1', // или VaultType.VAULT_1
  durationMonths: 12,
  minEntryUsdt: 100,

  // ⚠️ ПРАВИЛЬНЫЕ ЗНАЧЕНИЯ APY:
  baseApyBps: 450,      // 4.5% Start APY
  boostApyBps: 400,     // 4.0% Laika Boost
  maxApyBps: 850,       // 8.5% Max APY

  // или если в процентах:
  baseApy: 4.5,
  boostApy: 4.0,
  maxApy: 8.5,

  takaraMiningAprBps: 3000, // 30%
  boostToken: 'LAIKA',
  boostRatio: 0.40,
  boostPriceDiscount: 0.15,
}

// VAULT 2: Advanced
{
  name: 'Advanced',
  type: 'VAULT_2',
  durationMonths: 30,
  minEntryUsdt: 1500,

  // ⚠️ ПРАВИЛЬНЫЕ ЗНАЧЕНИЯ APY:
  baseApyBps: 700,      // 7% Start APY
  boostApyBps: 600,     // 6% Takara Boost
  maxApyBps: 1300,      // 13% Max APY

  takaraMiningAprBps: 3000, // 30%
  boostToken: 'TAKARA',
  boostRatio: 1.0,
  boostPriceFixed: 0.10,
}

// VAULT 3: Whale
{
  name: 'Whale',
  type: 'VAULT_3',
  durationMonths: 36,
  minEntryUsdt: 5000,

  // ⚠️ ПРАВИЛЬНЫЕ ЗНАЧЕНИЯ APY:
  baseApyBps: 800,      // 8% Start APY
  boostApyBps: 700,     // 7% Takara Boost
  maxApyBps: 1500,      // 15% Max APY

  takaraMiningAprBps: 4000, // 40%
  boostToken: 'TAKARA',
  boostRatio: 1.0,
  boostPriceFixed: 0.10,
}
```

После обновления:

```bash
# Пересоздать seed
npx prisma db seed

# Или если нужно обновить существующие записи:
npx prisma db push --force-reset && npx prisma db seed
```

---

## ⚙️ ЧАСТЬ 3: BACKEND AUDIT

### 3.1 Startup Check

```bash
# 1. Установить зависимости
npm install
# или
pnpm install

# 2. Проверить что билд проходит
npm run build

# 3. Запустить в dev режиме и проверить логи
npm run dev

# 4. Проверить health endpoint
curl http://localhost:3001/health
# или
curl http://localhost:3001/api/health
```

### 3.2 Checklist для Backend

#### Структура и конфигурация:

- [ ] `npm install` проходит без ошибок
- [ ] `npm run build` проходит без ошибок
- [ ] `npm run dev` запускается без ошибок
- [ ] Все env variables установлены корректно
- [ ] Подключение к БД работает
- [ ] Порт не занят другим процессом

#### API Endpoints - проверить КАЖДЫЙ:

```bash
# Примеры curl запросов для тестирования:

# GET all vaults
curl -X GET http://localhost:3001/api/vaults

# GET single vault
curl -X GET http://localhost:3001/api/vaults/1

# GET user deposits (с auth если нужен)
curl -X GET http://localhost:3001/api/deposits \
  -H "Authorization: Bearer TOKEN"

# POST create deposit (тестовый)
curl -X POST http://localhost:3001/api/deposits \
  -H "Content-Type: application/json" \
  -d '{"vaultId": 1, "amount": 100}'

# И так далее для ВСЕХ endpoints...
```

#### Checklist по endpoints:

**Vaults/Pools:**

- [ ] GET /api/vaults — возвращает список с ПРАВИЛЬНЫМИ APY
- [ ] GET /api/vaults/:id — возвращает конкретный vault
- [ ] Данные APY корректны в response

**Deposits:**

- [ ] GET /api/deposits — возвращает депозиты пользователя
- [ ] POST /api/deposits — создаёт новый депозит
- [ ] GET /api/deposits/:id — возвращает конкретный депозит
- [ ] Валидация минимальной суммы работает
- [ ] Валидация vault ID работает

**Claims:**

- [ ] POST /api/claims/usdt — клейм USDT rewards
- [ ] POST /api/claims/takara — клейм Takara rewards
- [ ] Проверка прав доступа работает
- [ ] Расчёт rewards корректен

**Auth (если есть):**

- [ ] POST /api/auth/connect-wallet — подключение кошелька
- [ ] GET /api/auth/me — текущий пользователь
- [ ] Middleware проверки auth работает

**Calculator:**

- [ ] GET/POST /api/calculator — расчёт доходности
- [ ] Формулы расчёта корректны
- [ ] Учитываются frequency multipliers

### 3.3 Частые ошибки Backend и их исправления

#### Ошибка 1: Prisma Client not generated

```
Error: @prisma/client did not initialize yet
```

**Исправление:**

```bash
npx prisma generate
```

#### Ошибка 2: Database connection failed

```
Error: Can't reach database server
```

**Исправление:**

- Проверить DATABASE_URL в .env
- Проверить что PostgreSQL запущен
- Проверить firewall/network

#### Ошибка 3: Module not found

```
Error: Cannot find module 'xxx'
```

**Исправление:**

```bash
rm -rf node_modules
npm install
```

#### Ошибка 4: Port already in use

```
Error: EADDRINUSE
```

**Исправление:**

```bash
# Найти процесс
lsof -i :3001
# Убить процесс
kill -9 PID
```

### 3.4 Проверка Services Logic

Найди и проверь следующие сервисы:

#### VaultService / PoolService:

```typescript
// Проверить что сервис возвращает правильные APY

// НАЙТИ метод типа:
async getVaults() {
  return this.prisma.vault.findMany();
}

// УБЕДИТЬСЯ что данные из БД корректны
// или что есть трансформация в правильный формат
```

#### YieldCalculatorService:

```typescript
// Проверить формулы расчёта

// Формула для APY:
// totalApy = baseApy + (hasBoost ? boostApy : 0)
// effectiveApy = totalApy * frequencyMultiplier

// Frequency Multipliers:
// MONTHLY: 1.0
// QUARTERLY: 1.15
// YEARLY: 1.30

// Пример для Vault 1 с Yearly и бустом:
// effectiveApy = (4.5 + 4.0) * 1.30 = 11.05%
```

#### DepositService:

```typescript
// Проверить валидации:

// 1. Минимальная сумма
if (amount < vault.minEntryUsdt) {
  throw new BadRequestException("Amount below minimum");
}

// 2. Vault существует
const vault = await this.prisma.vault.findUnique({ where: { id: vaultId } });
if (!vault) {
  throw new NotFoundException("Vault not found");
}

// 3. Создание депозита с правильными данными
```

---

## 🖥️ ЧАСТЬ 4: FRONTEND AUDIT

### 4.1 Общая проверка

```bash
# 1. Установить зависимости
cd frontend  # или где находится frontend
npm install

# 2. Проверить билд
npm run build

# 3. Запустить dev сервер
npm run dev

# 4. Открыть в браузере
# http://localhost:3000
```

### 4.2 Checklist по страницам

Открой КАЖДУЮ страницу и проверь:

#### Главная страница (/):

- [ ] Страница загружается без ошибок
- [ ] Навигация работает
- [ ] Все ссылки кликабельны
- [ ] Нет ошибок в console

#### Страница Vaults (/vaults):

- [ ] Список vaults загружается
- [ ] APY отображаются ПРАВИЛЬНО:
  - [ ] Vault 1: "4.5% — 8.5%"
  - [ ] Vault 2: "7% — 13%"
  - [ ] Vault 3: "8% — 15%"
- [ ] Кнопка "Deposit" на каждой карточке работает
- [ ] Кнопка "Details" работает (если есть)
- [ ] Progress bar заполнения batch отображается (если есть)

#### Страница Deposit (/deposit или modal):

- [ ] Форма отображается
- [ ] Input для суммы работает
- [ ] Валидация минимальной суммы работает
- [ ] Выбор payout frequency работает
- [ ] Toggle для буста работает
- [ ] Калькулятор показывает правильные цифры
- [ ] Кнопка "Connect Wallet" работает
- [ ] Кнопка "Deposit" работает (или показывает что нужен кошелёк)

#### Dashboard (/dashboard):

- [ ] Страница загружается
- [ ] Депозиты пользователя отображаются
- [ ] APY каждого депозита корректен
- [ ] Pending rewards отображаются
- [ ] Кнопка "Claim" работает
- [ ] История claims отображается (если есть)

#### Calculator (/calculator если отдельная страница):

- [ ] Калькулятор загружается
- [ ] Выбор vault работает
- [ ] Input суммы работает
- [ ] Выбор срока работает
- [ ] Выбор frequency работает
- [ ] Toggle буста работает
- [ ] Результаты рассчитываются правильно

### 4.3 Checklist по компонентам

#### Кнопки (Buttons):

Проверь КАЖДУЮ кнопку на сайте:

| Кнопка         | Страница   | Работает? | Проблема |
| -------------- | ---------- | --------- | -------- |
| Connect Wallet | Header     | [ ]       |          |
| Deposit        | Vault Card | [ ]       |          |
| Claim USDT     | Dashboard  | [ ]       |          |
| Claim Takara   | Dashboard  | [ ]       |          |
| Calculate      | Calculator | [ ]       |          |
| ...            | ...        | [ ]       |          |

#### Формы (Forms):

- [ ] Все inputs принимают ввод
- [ ] Валидация работает (показывает ошибки)
- [ ] Submit отправляет данные
- [ ] Loading state отображается
- [ ] Success/Error состояния показываются

#### Modals:

- [ ] Открываются по клику
- [ ] Закрываются по клику на X
- [ ] Закрываются по клику вне modal
- [ ] Закрываются по Escape
- [ ] Контент внутри корректен

#### Wallet Connection:

- [ ] Кнопка "Connect Wallet" видна
- [ ] При клике открывается выбор кошелька
- [ ] TronLink определяется (если установлен)
- [ ] Phantom/Solflare определяется (если установлен)
- [ ] После подключения адрес отображается
- [ ] Кнопка "Disconnect" работает

### 4.4 Console Errors Check

Открой DevTools (F12) → Console и проверь на КАЖДОЙ странице:

```
⚠️ Типичные ошибки которые нужно исправить:

❌ TypeError: Cannot read property 'xxx' of undefined
   → Данные не загружены, нужен optional chaining или проверка

❌ 404 (Not Found) для API запросов
   → Неправильный URL endpoint или endpoint не существует

❌ 500 (Internal Server Error)
   → Ошибка на бэкенде, смотреть логи сервера

❌ CORS error
   → Настроить CORS на бэкенде

❌ Failed to fetch
   → Бэкенд не запущен или неправильный URL

❌ Hydration error (Next.js)
   → Server/Client mismatch, проверить useEffect
```

### 4.5 Network Requests Check

DevTools → Network tab:

- [ ] API запросы уходят на правильный URL
- [ ] Ответы приходят с status 200
- [ ] Данные в response корректны
- [ ] Нет зависших запросов
- [ ] Нет дублирующихся запросов

### 4.6 Частые ошибки Frontend и их исправления

#### Ошибка 1: Кнопка не реагирует на клик

```tsx
// ПРОБЛЕМА: onClick не привязан или функция не определена

// НАЙТИ:
<button>Click me</button>  // ❌ нет onClick

// ИСПРАВИТЬ:
<button onClick={handleClick}>Click me</button>  // ✅

// Или если функция не определена:
const handleClick = () => {
  console.log('clicked');
  // логика
};
```

#### Ошибка 2: Данные не отображаются

```tsx
// ПРОБЛЕМА: Данные undefined, нет loading state

// НАЙТИ:
{
  vaults.map((v) => <Card key={v.id} />);
} // ❌ vaults может быть undefined

// ИСПРАВИТЬ:
{
  isLoading && <Spinner />;
}
{
  error && <Error message={error} />;
}
{
  vaults?.map((v) => <Card key={v.id} />);
} // ✅ optional chaining
```

#### Ошибка 3: Форма не отправляется

```tsx
// ПРОБЛЕМА: form submit не обрабатывается

// НАЙТИ:
<form>
  <input />
  <button type="submit">Submit</button>  // ❌ нет onSubmit
</form>

// ИСПРАВИТЬ:
<form onSubmit={handleSubmit}>
  <input value={value} onChange={e => setValue(e.target.value)} />
  <button type="submit">Submit</button>
</form>

const handleSubmit = async (e) => {
  e.preventDefault();  // ⚠️ Важно!
  // отправка данных
};
```

#### Ошибка 4: Неправильные APY отображаются

```tsx
// НАЙТИ где отображаются APY и исправить:

// Если хардкод:
<span>APY: 4%</span>  // ❌

// ИСПРАВИТЬ на данные из API:
<span>APY: {vault.baseApy}%</span>  // ✅

// Или если нужно форматирование:
<span>APY: {(vault.baseApyBps / 100).toFixed(1)}%</span>
```

#### Ошибка 5: Wallet не подключается

```tsx
// Проверить что wallet adapter настроен правильно:

// Для Solana:
import { useWallet } from "@solana/wallet-adapter-react";

const { connect, connected, publicKey } = useWallet();

// Для Tron:
// Проверить что TronLink установлен
if (window.tronWeb && window.tronWeb.ready) {
  const address = window.tronWeb.defaultAddress.base58;
}
```

---

## 🔗 ЧАСТЬ 5: ИНТЕГРАЦИОННЫЙ АУДИТ

### 5.1 End-to-End Flow Testing

Протестируй полный флоу пользователя:

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    USER FLOW TEST SCENARIO                               │
└─────────────────────────────────────────────────────────────────────────┘

SCENARIO 1: Просмотр Vaults (без кошелька)
1. Открыть /vaults
2. ✓ Видеть список 3 vaults
3. ✓ APY отображаются правильно
4. ✓ Кликнуть на "Details" — открывается детали
5. ✓ Кликнуть на "Deposit" — предлагает подключить кошелёк

SCENARIO 2: Подключение кошелька
1. Кликнуть "Connect Wallet"
2. ✓ Открывается modal выбора кошелька
3. ✓ Выбрать Phantom/TronLink
4. ✓ Кошелёк подключается
5. ✓ Адрес отображается в header
6. ✓ Статус "Connected" виден

SCENARIO 3: Создание депозита (mock без реальной транзакции)
1. Подключить кошелёк
2. Открыть Vault 1
3. Кликнуть "Deposit"
4. ✓ Открывается форма депозита
5. Ввести сумму $100
6. ✓ Калькулятор показывает ожидаемый доход
7. Выбрать Quarterly payout
8. ✓ Множитель x1.15 применяется
9. Включить Laika boost
10. ✓ Показывает сколько Laika нужно
11. Кликнуть "Deposit"
12. ✓ Показывается loading state
13. ✓ Или показывается что нужна транзакция (mock режим)

SCENARIO 4: Dashboard (с mock данными)
1. Открыть /dashboard
2. ✓ Показываются депозиты пользователя
3. ✓ Pending rewards отображаются
4. Кликнуть "Claim Takara"
5. ✓ Показывается результат (mock или pending state)
```

### 5.2 API Integration Checklist

- [ ] Frontend вызывает правильные endpoints
- [ ] Headers (Authorization, Content-Type) корректны
- [ ] Request body формируется правильно
- [ ] Response обрабатывается корректно
- [ ] Ошибки отображаются пользователю
- [ ] Loading states работают

---

## 🛠️ ЧАСТЬ 6: MOCK MODE (для работы без смарт-контрактов)

Пока смарт-контракты не задеплоены, платформа должна работать в MOCK режиме.

### 6.1 Что нужно замокать

```typescript
// config/features.ts или .env

export const FEATURES = {
  // Включить mock режим пока нет контрактов
  MOCK_BLOCKCHAIN: true,

  // Позволять создавать депозиты без реальных транзакций
  MOCK_DEPOSITS: true,

  // Показывать mock rewards
  MOCK_REWARDS: true,
};
```

### 6.2 Mock для Deposit Flow

```typescript
// services/deposit.service.ts

async createDeposit(dto: CreateDepositDto) {
  if (FEATURES.MOCK_BLOCKCHAIN) {
    // Mock режим - создаём запись без реальной транзакции
    const deposit = await this.prisma.deposit.create({
      data: {
        ...dto,
        status: 'MOCK_ACTIVE', // или 'ACTIVE'
        tronTxHash: 'MOCK_TX_' + Date.now(),
        wexelMintAddress: 'MOCK_WEXEL_' + Date.now(),
        lockStartAt: new Date(),
        lockEndAt: new Date(Date.now() + dto.vault.durationMonths * 30 * 24 * 60 * 60 * 1000),
      },
    });

    return {
      success: true,
      deposit,
      message: 'Deposit created in MOCK mode. Real blockchain transaction not required.',
    };
  }

  // Real blockchain flow
  // ...
}
```

### 6.3 Mock для Claims

```typescript
// services/claims.service.ts

async claimTakara(depositId: number, userId: string) {
  if (FEATURES.MOCK_BLOCKCHAIN) {
    // Рассчитать pending rewards
    const pending = await this.calculatePendingTakara(depositId);

    // Обновить запись
    await this.prisma.deposit.update({
      where: { id: depositId },
      data: {
        takaraClaimedTotal: { increment: pending },
        lastTakaraClaimAt: new Date(),
      },
    });

    return {
      success: true,
      claimed: pending,
      message: 'Claim processed in MOCK mode.',
    };
  }

  // Real blockchain claim
  // ...
}
```

### 6.4 Frontend Mock Indicators

```tsx
// components/MockBanner.tsx

export function MockBanner() {
  if (!FEATURES.MOCK_BLOCKCHAIN) return null;

  return (
    <div className="bg-yellow-500 text-black px-4 py-2 text-center">
      ⚠️ Platform is running in TEST MODE. Transactions are simulated.
    </div>
  );
}

// Добавить в layout
<MockBanner />;
```

---

## 📝 ЧАСТЬ 7: СПИСОК ТИПИЧНЫХ ПРОБЛЕМ И РЕШЕНИЙ

### 7.1 Backend Issues

| #   | Проблема                    | Решение                                      |
| --- | --------------------------- | -------------------------------------------- |
| 1   | API возвращает 500          | Проверить логи, исправить ошибку в сервисе   |
| 2   | Prisma Client error         | `npx prisma generate`                        |
| 3   | DB connection failed        | Проверить DATABASE_URL, запустить PostgreSQL |
| 4   | CORS error                  | Добавить CORS middleware с правильным origin |
| 5   | Неправильные APY в response | Обновить seed data и пересидировать БД       |
| 6   | Auth не работает            | Проверить JWT secret, token validation       |
| 7   | Validation fails            | Проверить DTO, class-validator decorators    |

### 7.2 Frontend Issues

| #   | Проблема               | Решение                                         |
| --- | ---------------------- | ----------------------------------------------- |
| 1   | Кнопка не работает     | Добавить onClick handler                        |
| 2   | Данные не загружаются  | Проверить API URL, добавить error handling      |
| 3   | Console errors         | Исправить по сообщению ошибки                   |
| 4   | Blank page             | Проверить React errors, добавить Error Boundary |
| 5   | Стили сломаны          | Проверить Tailwind config, CSS imports          |
| 6   | Wallet не подключается | Проверить adapter config, наличие расширения    |
| 7   | Форма не отправляется  | Добавить onSubmit, preventDefault               |
| 8   | Неправильные цифры     | Исправить формулы расчёта, проверить данные API |

### 7.3 Integration Issues

| #   | Проблема                  | Решение                                |
| --- | ------------------------- | -------------------------------------- |
| 1   | Frontend не видит Backend | Проверить NEXT_PUBLIC_API_URL          |
| 2   | Requests pending forever  | Backend не отвечает, проверить логи    |
| 3   | Data mismatch             | Синхронизировать типы Frontend/Backend |
| 4   | Auth token не передаётся  | Добавить Authorization header          |

---

## ✅ ЧАСТЬ 8: ФИНАЛЬНЫЙ CHECKLIST

После всех исправлений проверь:

### Database ✓

- [ ] Schema валидна
- [ ] Все миграции применены
- [ ] APY values правильные (4.5/4, 7/6, 8/7)
- [ ] Seed data корректен

### Backend ✓

- [ ] Билд проходит без ошибок
- [ ] Все endpoints отвечают
- [ ] APY в responses правильные
- [ ] Error handling работает
- [ ] Валидация работает

### Frontend ✓

- [ ] Билд проходит без ошибок
- [ ] Все страницы загружаются
- [ ] Все кнопки работают
- [ ] Все формы работают
- [ ] Console без ошибок
- [ ] APY отображаются правильно
- [ ] Wallet connection работает
- [ ] Mock mode работает

### Integration ✓

- [ ] Frontend ↔ Backend связь работает
- [ ] Full user flow проходит
- [ ] Все features доступны
- [ ] Responsive design работает

---

## 🚀 ЧАСТЬ 9: КОМАНДЫ ДЛЯ ЗАПУСКА АУДИТА

Выполни последовательно:

```bash
# 1. Клонировать репо (если нужно)
git clone https://github.com/olegin77/TUSD.git
cd TUSD

# 2. Установить зависимости
pnpm install  # или npm install

# 3. Проверить и обновить .env
cat .env
# Убедиться что DATABASE_URL и другие переменные установлены

# 4. Database setup
npx prisma generate
npx prisma migrate deploy  # или dev
npx prisma db seed

# 5. Проверить APY в БД
npx prisma studio
# Открыть таблицу Vault и проверить значения APY

# 6. Запустить backend
npm run dev  # в одном терминале

# 7. Запустить frontend
cd frontend  # если отдельная папка
npm run dev  # в другом терминале

# 8. Открыть браузер и начать тестирование
# http://localhost:3000

# 9. Открыть DevTools (F12) и следить за Console и Network
```

---

## 📋 ОТЧЁТ ОБ АУДИТЕ

После завершения аудита, создай отчёт в формате:

```markdown
# TAKARA Platform Audit Report

## Date: [дата]

## Summary

- Total Issues Found: XX
- Critical: XX
- High: XX
- Medium: XX
- Low: XX
- Fixed: XX
- Pending: XX

## Issues Found

### Critical Issues

1. [Issue description] — FIXED ✅
2. [Issue description] — FIXED ✅

### High Issues

1. [Issue description] — FIXED ✅

### Medium Issues

1. [Issue description] — FIXED ✅

### Low Issues

1. [Issue description] — FIXED ✅

## APY Verification

- [ ] Vault 1: 4.5% / 4% / 8.5% — CORRECT ✅
- [ ] Vault 2: 7% / 6% / 13% — CORRECT ✅
- [ ] Vault 3: 8% / 7% / 15% — CORRECT ✅

## Pages Tested

- [ ] / — Works ✅
- [ ] /vaults — Works ✅
- [ ] /dashboard — Works ✅
- [ ] /deposit — Works ✅

## Buttons Tested

- [ ] Connect Wallet — Works ✅
- [ ] Deposit — Works ✅
- [ ] Claim — Works ✅
- [ ] ... — Works ✅

## Conclusion

Platform is now at XX% working state.
```

---

**END OF AUDIT & FIX DOCUMENT**

Приступай к аудиту. Начни с проверки environment и database, затем переходи к backend и frontend. Исправляй проблемы по мере их обнаружения и документируй всё.
